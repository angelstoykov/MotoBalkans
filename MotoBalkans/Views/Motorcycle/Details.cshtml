@using MotoBalkans.Web.Data.Enums
@model MotorcycleDetailsViewModel
@{
    ViewBag.Title = "Motorcycle Details";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="row">
    <div class="card w-100">
        <div class="card-body">
            <h3 class="card-title text-center">@Model.Brand</h3>
            <h3 class="card-title text-center">@Model.Model</h3>
            <h3 class="card-title text-center">@Model.EngineType</h3>
        </div>

        <a asp-controller="Motorcycle" asp-action="All" class="btn btn-warning mb-2 w-100 p-3 fw-bold">Back to All Motocycles</a>
        @* @if (User?.Identity?.Name == Model.Organizer)
        {
            <a asp-controller="Seminar" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning mb-2 w-100 p-3 fw-bold">Edit</a>
            <a asp-controller="Seminar" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger mb-2 w-100 p-3 fw-bold">Delete</a>
        } *@

    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
